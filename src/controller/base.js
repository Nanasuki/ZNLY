module.exports = class extends think.Controller {
  async __before() {
    // const sessionScId = await this.post('SC_ID');
    // if (typeof (sessionScId) === 'undefined' || sessionScId == null) {
    //   return false;
    // }
  }
};
